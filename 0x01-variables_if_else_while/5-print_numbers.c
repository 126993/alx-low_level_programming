#include <stdio.h>
/**
 * main - Aprogram that prints all single digits of base 10.
 *
 * Return: 0 (success)
 */
int main(void)
{
int num;
while (num < 10)
{
printf("%d", num);
num += 1;
}
printf("\n");
return (0);
}
